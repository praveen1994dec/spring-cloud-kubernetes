kind: Service
apiVersion: v1
metadata:
  name: kubernetes-configmap-reload1
spec:
  type: LoadBalancer
  selector:
    app: kubernetes-configmap-reload1
  ports:
    - name: http
      protocol: TCP
      # ELB's port
      port: 8081
